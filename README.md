# Phase 2: SFT Warm-Up

This phase performs supervised fine-tuning (SFT) as a warm-up step before GRPO. The goal is to adapt the raw `Qwen/Qwen2.5-1.5B` base model toward structured mathematical reasoning outputs using chain-of-thought style examples from NuminaMath-CoT.

Unlike an instruction-tuned model, the raw Qwen base model is primarily a text-completion model. An initial chat-style formatting approach was unstable, so this phase uses a simpler plain-completion format:

```text
Problem:
{problem}

Solution:
<think>
{reasoning}
</think>
The final answer is {answer}
```

This format produced more stable outputs than chat tokens such as `<|im_start|>user` and `<|im_start|>assistant`.

## What This Phase Does

- Loads and filters the NuminaMath-CoT dataset.
- Removes examples that are too long, too symbolic, non-English, multiple-choice, or competition-style.
- Converts examples into a plain `Problem/Solution` SFT format.
- Fine-tunes `Qwen/Qwen2.5-1.5B` using LoRA.
- Saves the SFT checkpoint to:

```text
./checkpoints/sft_plain_base/final
```

- Runs a small GSM8K evaluation to measure:
  - exact final-answer accuracy
  - complete `<think>...</think>` format rate

## Files

```text
notebooks/phase2_sft_warmup.ipynb
```

Main Phase 2 notebook. It contains dataset preparation, LoRA SFT training, qualitative sample generation, and SFT-only evaluation.

```text
scripts/phase2_sft_warmup.py
```

Python script export of the Phase 2 notebook for easier code review and reproducibility.

Expected output files:

```text
./data/sft/train
./data/sft/val
./data/sft/prep_summary.json
./data/sft/sample_formatted_examples.json
./checkpoints/sft_plain_base/final
./checkpoints/sft_plain_base/training_config.json
./checkpoints/sft_plain_base/train_result.json
./checkpoints/sft_plain_base/trainer_log_history.json
./checkpoints/sft_plain_base/sample_generations.json
./sft_eval_summary.json
./sft_eval_results_5_plain_base.json
```

The checkpoint directory is not included in GitHub because model weights are large. It can be regenerated by running the notebook or script.

## Setup

Install the required libraries:

```bash
pip install -r requirements.txt
```

The notebook was developed with the following main package versions:

```text
datasets==2.21.0
transformers==4.46.3
peft==0.13.2
trl==0.12.2
accelerate==1.1.1
pyarrow==16.1.0
huggingface_hub==0.36.2
```

## How To Run

Open the notebook:

```text
notebooks/phase2_sft_warmup.ipynb
```

Run the cells from top to bottom.

The notebook has three main sections:

1. Dataset preparation
2. SFT training
3. SFT-only evaluation

### 1. Dataset Preparation

This section downloads NuminaMath-CoT, filters examples, formats them for SFT, and saves the processed dataset to:

```text
./data/sft/train
./data/sft/val
```

### 2. SFT Training

This section loads the base model:

```text
Qwen/Qwen2.5-1.5B
```

and applies LoRA adapters for efficient fine-tuning.

The final training configuration uses:

```text
train examples: 1500
validation examples: 150
epochs: 1
LoRA rank: 16
LoRA alpha: 32
max sequence length: 768
```

To run a quick smoke test, set:

```python
SMALL_TEST = True
```

To reproduce the final Phase 2 run, set:

```python
SMALL_TEST = False
```

The trained model is saved to:

```text
./checkpoints/sft_plain_base/final
```

### 3. SFT-Only Evaluation

The notebook evaluates the SFT checkpoint on a small GSM8K subset:

```python
eval_ds = load_dataset("gsm8k", "main", split="test[:10]")
```

The reported evaluation uses 5 examples to conserve GPU time.

The final Phase 2 result was:

```text
Accuracy: 0.0
Complete think-tag rate: 0.2
```

## Running The Python Script

The Python script version can be run with:

```bash
python scripts/phase2_sft_warmup.py
```

The script follows the same pipeline as the notebook:

1. Prepare the SFT dataset.
2. Train the LoRA SFT model.
3. Save the checkpoint and evaluation outputs.

For interactive debugging and reviewing outputs, the notebook is recommended. For code review and reproducibility, the script is provided as a cleaner exported version.

## Phase 2 Takeaway

The SFT warm-up improved output stability compared with the original chat-style format. The model became more likely to generate math-solution-style text and `<think>` blocks. However, exact-answer accuracy remained poor under the limited-compute setting. The model frequently made arithmetic mistakes, changed numbers from the original problem, or failed to complete the reasoning block.

This result motivates Phase 3 GRPO, where verifiable answer rewards and format rewards can directly optimize the behaviors that SFT alone did not reliably learn.
